# Backend-only PostgreSQL connection. Use the Supabase session pooler for a
# deployed IPv4 backend, or the direct connection for migrations/local IPv6.
DATABASE_URL=postgresql+psycopg://postgres.atxpojdzqvdeisvxlgwj:YOUR_PASSWORD@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require

# Public backend URL compiled into the Vite frontend. Never put DB secrets here.
VITE_API_BASE_URL=http://localhost:8000
